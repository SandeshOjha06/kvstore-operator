# This rule is not used by the project kvstore-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to datastore.core.systems resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: kvstore-operator
    app.kubernetes.io/managed-by: kustomize
  name: kvstore-viewer-role
rules:
- apiGroups:
  - datastore.core.systems
  resources:
  - kvstores
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - datastore.core.systems
  resources:
  - kvstores/status
  verbs:
  - get
